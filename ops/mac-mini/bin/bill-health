#!/bin/bash
set -euo pipefail
source "$(dirname -- "$0")/_bill-common.sh"
root="$(bill_repo_root)"
load_bill_env
ensure_bill_runtime_dirs
node_bin="$(bill_node)"
(
  cd "$root"
  "$node_bin" ops/mac-mini/scripts/health.mjs "$@"
)
