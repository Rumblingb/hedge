#!/bin/bash
set -euo pipefail
source "$(dirname -- "$0")/_bill-common.sh"
load_bill_env
journal_path="${BILL_PREDICTION_JOURNAL_PATH:-.rumbling-hedge/runtime/prediction/opportunities.jsonl}"
run_bill_cli prediction-report "$journal_path"
