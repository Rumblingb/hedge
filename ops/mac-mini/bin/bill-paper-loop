#!/bin/bash
set -euo pipefail
source "$(dirname -- "$0")/_bill-common.sh"
load_bill_env
if [[ "${BILL_ENABLE_PAPER_LOOP:-false}" != "true" ]]; then
  printf '{\n  "command": "bill-paper-loop",\n  "status": "skipped",\n  "reason": "BILL_ENABLE_PAPER_LOOP is not true"\n}\n'
  exit 0
fi
run_bill_cli demo-tomorrow "$@"
