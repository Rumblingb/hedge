EOD review for the Bill/Hedge agentic fund (campaign to 2026-06-22). Work in /Users/brain/hedge, token-frugal (<=15 tool calls), no subagents.
1. Broker truth (read-only env trio RH_TOPSTEP_READ_ONLY=true BILL_ENABLE_FUTURES_DEMO_EXECUTION=false RH_LIVE_EXECUTION_ENABLED=false): run `~/.hermes/scripts/topstep_demo_fill_check_run.sh` then `.venv/bin/python scripts/trade_journal.py` with the same env. If today had fills missing from .rumbling-hedge/state/trade-journal.jsonl, query /api/Trade/search using the login pattern in ~/.hermes/scripts/topstep_demo_fill_check.py and append accurate JSONL entries (match existing schema; real fill timestamps and P&L).
2. Compute KPIs from the journal: n, win rate, profit factor, total P&L, average duration. Flag any sub-3-minute trade as a discipline violation.
3. Append an EOD section to today's plan in ~/Documents/memorybrain/Agent-Hermes/daily/: trades + P&L, KPI line, which sessions actually ticked (master-signal.latest.json + hermes cron last_run times for master-strategy-bridge).
4. Append one row to the daily log table in ~/Documents/memorybrain/Agent-Hermes/BILL-12-DAY-CAMPAIGN-2026-06.md: date | sessions covered | trades | P&L | PF-to-date | open blockers | note.
5. Review triggers: 3 consecutive losing days OR PF < 0.8 with n>=10 → write a loud size-halving/pause recommendation in the campaign doc AND draft tomorrow's plan with BILL_ROUTE_APPROVAL: BLOCKED + reason. Never weaken a gate.
